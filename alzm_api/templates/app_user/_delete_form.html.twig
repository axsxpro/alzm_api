<form method="post" action="{{ path('app_user_delete', {'idUser': app_user.idUser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ app_user.idUser) }}">
    <button class="btn">Delete</button>
</form>
